[project]
name = "webexrssincbot"
version = "0.1.0"
description = "Parse specific WebEx Service RSS feeds and post incidents to a WebEx space using WebEx bot"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "feedparser>=6.0.12",
    "requests>=2.34.2",
]
