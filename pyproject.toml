[project]
name = "webexrssincbot"
version = "0.2.0"
description = "Parse specific WebEx Service RSS feeds and Security advisories and post to a WebEx space using WebEx bot"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "feedparser>=6.0.12",
    "requests>=2.34.2",
]
